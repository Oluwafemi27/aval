# Zero-configuration loop illustration

This source example is illustrative: `orbit.avl` and `orbit.png` are asset
placeholders and are not included here. Supply those files (or change the
paths), then run `npm install` and `npm run build`.

For an immediately runnable generated asset and browser workflow, use
`avl init` and the checked starter instead.
