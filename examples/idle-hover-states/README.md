# Idle/hover states illustration

This source example is illustrative: `favorite.avl` is an asset placeholder
and is not included here. Supply an asset with the authored `idle` and
`selected` states (or change the code), then run `npm install` and
`npm run build`.

For an immediately runnable generated idle/engaged asset and browser workflow,
use `avl init` and the checked starter instead.
